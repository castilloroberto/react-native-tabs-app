import React from 'react'
import { Text } from 'react-native'
import { Center } from '../components/Center'

interface RecargasProps {

}

export const Recargas: React.FC<RecargasProps> = ({}) => {
        return (
            <Center>
                <Text>Recargas</Text>
            </Center>
        )
}